//IMPORTS REACT
import React from 'react'

function Sidebar({children}) {
    // RENDERED CONTENT
    return (
        <div className="Sidebar">
            {children}
        </div>

    )
}
// EXPORTS COMPONENT
export default Sidebar;